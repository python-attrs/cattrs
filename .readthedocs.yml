version: 2

build:
  os: ubuntu-20.04
  tools:
    # Keep version in sync with tox.ini (docs and gh-actions).
    python: "3.11"

build:
  jobs:
    post_create_environment:
      - pip install pdm
    post_install:
      - pdm install -G :all,docs
